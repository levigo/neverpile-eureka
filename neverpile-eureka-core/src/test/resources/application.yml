spring:
  jackson.serialization.WRITE_DATES_AS_TIMESTAMPS: false

neverpile:
  url-crypto:
    shared-secret:
      enabled: true
      secret-key: default-encryption-secret

spring.web.error.include-message: always
